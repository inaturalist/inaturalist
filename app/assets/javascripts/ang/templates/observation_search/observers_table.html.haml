#observers-table.container{ "infinite-scroll": "showMoreObservers( )", "infinite-scroll-disabled": "!viewing('observers')", "infinite-scroll-distance": 1 }
  %table.table.table-striped.plain-table
    %thead
      %tr
        %th.rank
          {{ shared.t( 'rank' )}}
        %th.user
          {{ shared.t( 'user' )}}
        %th{ "ng-click": "observersSort = 'observationCount';" }
          %i.fa.fa-binoculars
          {{ shared.t( 'observations' )}}
        %th{ "ng-click": "observersSort = 'speciesCount';" }
          {{ shared.t( 'species' )}}
    %tbody
      %tr{ "ng-repeat": "u in observers | orderBy:observersSort:true | limitTo: numberObserversShown" }
        %td.rank
          {{ $index + 1 }}
        %td
          %a.user.userimage{ href: "/people/{{ u.login }}", style: "{{ shared.backgroundIf( u.icon_url ) }}", title: "{{ u.login }}" }
            %i.icon-person{"ng-hide" => "u.icon_url"}
          %a{ href: "/people/{{ u.login }}", title: "{{ u.login }}" }
            {{ u.login }}
        %td {{ shared.numberWithCommas( u.observationCount ) }}
        %td {{ shared.numberWithCommas( u.speciesCount ) }}
