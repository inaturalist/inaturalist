<%= yield %>

-----------------------------------------------------------------------------
<%=plaintext_t :inaturalist_is_an_independent_nonprofit %>

<% if @user %>
<%=plaintext_t :you_received_this_email_notice, email: @user.email, username: @user.login, created_date: l( @user.created_at.to_date, format: :long ) %>
<% if @user.site && @user.site.id != Site.default.id %>
  <%=plaintext_t :your_account_is_affiliated, site_name: @user.site.name, site_url: @user.site.url %>
<% end -%>

<%- end %>
<%=plaintext_t :unsubscribe_from_emails_like_this %>
{{asm_group_unsubscribe_raw_url}}
