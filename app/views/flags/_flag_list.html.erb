<% unless flags.blank? %>
  <h3><%=t :existing_flags %></h3>
  <ul>
    <% if flags.empty? %>
      <li class="quiet description"><%=t :no_flags %></li>
    <% end %>

    <% for flag in flags %>
      <li>
        <%= render :partial => 'flag', :object => flag %>
      </li>
    <% end %>
  </ul>

<% end %>
