- extra ||= nil
- url_params = @observations_url_params || request.query_parameters || {}
.counts
  %label=t :totals
  .totalcount.stat.numerical
    .count ?
    = link_to t(:observations, :default => "observations").downcase, observations_path(url_params), :class => "label"
  .species.stat.numerical
    .count ?
    = link_to t(:species, :default => "species").downcase, taxa_observations_path(url_params), :class => "label"
  .people.stat.numerical
    .count ?
    = link_to t(:people, :default => "people").downcase, user_stats_observations_path(url_params), :class => "label"
  = extra
.most_observations.stat
  %label=t :most_observations
  %table
    %tr
      %td.image= image_tag('/attachment_defaults/users/icons/defaults/thumb.png')
      %td.user
        .meta=t :unknown
.most_species.stat
  %label=t :most_species
  %table
    %tr
      %td.image= image_tag('/attachment_defaults/users/icons/defaults/thumb.png')
      %td.user
        .meta=t :unknown
.most_observed.stat
  %label=t :most_observed_species
  %table
    %tr
      %td.image= image_tag('iconic_taxa/unknown-32px.png')
      %td.taxon
        .meta=t :unknown