<%= render :partial => 'header' %>

<%= t(:user_invite_observation_to_project,
      :user=> @inviter.login, 
      :observation => @observation.to_plain_s(:no_user => true),
      :project => @project.title %>

<%=t :you_can_accept_invitations%>: 

<%= observation_url(@observation) %>

<%= render :partial => 'footer' %>
