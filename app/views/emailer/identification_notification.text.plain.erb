<%=t :user_thinks_yours_is , :user=>@identifier.login,
  :noun => "#{ @identification.is_agreement? ? t(:agrees) : t(:thinks)}" , 
  :observation=> "#{@observation.to_plain_s(:no_user => true)}" %>

<%= render :partial => "taxa/taxon.txt.erb", :locals => { :taxon => @identification.taxon } %>
(<%= taxon_url(@identification.taxon) %>)

<% unless @identification.body.blank? %>"<%= @identification.body %>"<% end %>

<%=t :view_the_observation %>: <%= observation_url(@observation) %>

<%= render :partial => 'footer' %>
