- content_for(:title) do
  = t(:edit)
  = @announcement.id
.container
  .row
    .col-xs-12
      %span.breadcrumbs
        = link_to t(:back), @announcement, class: "back crumb"
      %h1= t :editing_announcement
      = render :partial => 'form'
