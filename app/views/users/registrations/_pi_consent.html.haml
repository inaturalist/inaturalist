#pi-consent.form-group.checkbox
  %label
    = f.check_box :pi_consent, required: true
    %span
      - if child
        =t :pi_consent_child_label
      - else 
        =t :pi_consent_label
      %button#pi-consent-button.btn.btn-nostyle{ type: "button", data: { toggle: "modal", target: "#pi-consent-modal" } }
        =t :learn_more
  #pi-consent-modal.modal.fade{ tabindex: -1, aria: { labelledby: "pi-consent-button" }}
    .modal-dialog{role: "document"}
      .modal-content
        .modal-header
          %button.close{:type => "button", "data-dismiss" => "modal"} &times;
          =t :pi_consent_desc_title
        .modal-body=t :pi_consent_desc_html, privacy_url: privacy_policy_url, terms_url: terms_of_service_url
