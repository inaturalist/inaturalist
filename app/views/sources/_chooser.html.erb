<%- source ||= chooser -%>
<strong><%= t("source_list.#{source.title.gsub(':','').gsub(' ','_').downcase}", :default=>source.title) %></strong>
<div class="small">
  <%= [source.in_text, source.url, t(:added).downcase + " #{source.created_at}"].delete_if{|s| s.blank?}.join(', ') %>
</div>
