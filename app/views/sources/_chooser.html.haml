- source ||= chooser
%strong= t("source_list.#{source.title.gsub( ":", "" ).gsub(" ", "_").downcase}", default: source.title )
.small
  = [source.in_text, source.url, t(:added_on_datetime, datetime: l( source.created_at ) )].delete_if{|s| s.blank?}.join( ", " )
