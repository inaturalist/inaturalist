= render "emailer/header"

%p=t :devise_confirmation_instructions_please_click
%p= link_to t( "devise.mailer.confirmation_instructions.subject" ), confirmation_url( @resource, confirmation_token: @token )
%p=t :devise_confirmation_instructions_if_you_did_not, site_name: @site.name, vow_or_con: @site.name[0].downcase

= render "emailer/footer"
