<%- content_for(:title) do -%>
  <%= t(:confirm_membership) %>
<%- end -%>
<% content_for(:extracss) { stylesheet_link_tag "projects/join" } %>
<div id="pageheader" class="clear">
  <div class="breadcrumbs">
    <strong><%= link_to "&laquo #{h @project.title}".html_safe, @project, class: "crumb" %></strong>
  </div><!-- /#breadcrumbs -->
  <h2><%=t :join_project?, project: @project.title %></h2>
</div>

<div class="project-join">
  <%= render "join" %>
</div>
