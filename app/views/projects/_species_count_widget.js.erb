try {
  var msg = document.getElementById('inatwidgetmsg');
  if (msg) {
    msg.style.visibility = 'visible';
  }
  document.write('<%= escape_javascript(render(:partial => "species_count_widget.html").gsub(/\n\s*/, '')) %>')
  
} catch (e) {}
