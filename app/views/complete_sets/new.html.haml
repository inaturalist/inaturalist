.container
  .row
    .col-xs-12
      %h2.pull-left
        = "New Complete Set"
  = form_for(@complete_set, :builder => DefaultFormBuilder, :html => {:multipart => true}) do |f|
    = f.error_messages
    = render :partial => 'form_fields', :locals => {:f => f}
    = f.submit t(:create), :class => "default button"
    = link_to t(:back), @complete_set.place, :class => "button"
