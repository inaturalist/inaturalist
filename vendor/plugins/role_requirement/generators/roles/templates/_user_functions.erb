  
  
  
  # ---------------------------------------
  # The following code has been generated by role_requirement.
  # You may wish to modify it to suit your need
  has_and_belongs_to_many :<%=roles_table_name%>
  
  # has_role? simply needs to return true or false whether a user has a role or not.  
  # It may be a good idea to have "admin" roles return true always
  def has_role?(role_in_question)
    @<%roles_table_name%>_list ||= self.<%=roles_table_name%>.collect(&:name)
    return true if @<%roles_table_name%>_list.include?("admin")
    (@<%roles_table_name%>_list.include?(role_in_question.to_s) )
  end
  # ---------------------------------------
  
  
  
  